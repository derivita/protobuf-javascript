/**
 * Ensures certain base libs are live so we can have an apples to apples
 * comparison for code size of different implementations
 */
declare function ensureCommonBaseLine(): void;
export { ensureCommonBaseLine };
